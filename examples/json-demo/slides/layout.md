# layout

in the manifest, `columns` is an array where each entry is either

- a **string** — a single-slide column
- an **array** — a column with depth (top first)
- an **object** — full control: `file`, `terminal`, `title`, `notes`, `notes_file`

there's a deeper slide below ↓
