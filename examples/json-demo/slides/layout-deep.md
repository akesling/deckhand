## per-slide files, same powers

files referenced from a manifest still support everything:

```terminal rows=8
echo "terminal blocks work here too"
```

and `???` notes — though the manifest's `notes` field wins if both exist
