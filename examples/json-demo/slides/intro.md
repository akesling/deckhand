# JSON decks

this presentation is laid out by `deck.json`

- one **markdown file per slide**
- the manifest decides where each slide sits on the 2-D grid
- a slide can be *just a terminal* — no markdown at all

???

Point out that the markdown files themselves stay plain — no separators needed.
