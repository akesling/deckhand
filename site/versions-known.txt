# Versions listed in versions.json beyond the v* git tags — for
# releases that predate tagging. One bare version per line. A version
# may only be listed once its snapshot directory (vX.Y.Z/) exists on
# the site-archive branch: deploys verify every listed version is
# actually served and refuse to publish otherwise. See site/README.md
# ("Backfilling a version") for how to add 0.1.0 / 0.2.0 here.
