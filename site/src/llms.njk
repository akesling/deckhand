---
# The body lives in _includes/llms.md, shared with /index.md — the
# landing page is njk/HTML (hero + wasm demo), so this index doubles as
# its markdown stand-in.
permalink: /llms.txt
eleventyExcludeFromCollections: true
---
{% include "llms.md" %}
