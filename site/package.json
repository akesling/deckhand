{
  "name": "deckhand-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "../scripts/build-wasm.sh",
    "build:ts": "bun build src/ts/demo.ts src/ts/play.ts src/ts/playground.ts --outdir dist/js --target browser --minify",
    "watch:ts": "bun build src/ts/demo.ts src/ts/play.ts src/ts/playground.ts --outdir dist/js --target browser --watch",
    "build:site": "bunx @11ty/eleventy",
    "build": "bun run build:wasm && bun run build:ts && bun run build:site",
    "dev": "bun run build:ts && bunx @11ty/eleventy --serve",
    "check": "bunx tsc --noEmit",
    "lint": "bunx biome lint --error-on-warnings ."
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "@biomejs/biome": "1.9.4",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.10.0",
    "@xterm/xterm": "5.5.0"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ]
}
